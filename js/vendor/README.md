# Vendor Runtime

Questa cartella ospita librerie runtime opzionali non bundle-izzate.

Per Lottie:

- Copia qui `lottie.min.js` (lottie-web).
- Path atteso dal bootstrap: `js/vendor/lottie.min.js`.

Se il file manca, l'app continua a funzionare usando il fallback immagine per i personaggi Lottie.
